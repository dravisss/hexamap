# Handoff contract

Report the project directory, map path, preview path, screenshot directory,
audit verdict, bundle path, preset, schema/workspace versions and every known
gap. Never claim visual acceptance from schema validation alone.
